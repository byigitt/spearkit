# spearkit starter

Scaffolded with `npx spearkit create`.

1. Copy `.env.example` to `.env` and set `DISCORD_TOKEN` and `GUILD_ID`.
2. `npm install` then `npm start`.
3. Import everything from `"spearkit"` — see `AGENTS.md`.
