# Everyday helpers

[`everyday.ts`](./everyday.ts) — `choices()`, `ctx.suggest()` for autocomplete,
and `ctx.sendLong()` for 2000-character splitting.

Also: `inviteUrl()`, `parseUserId()`, `ctx.dm()`, `ctx.withTyping()`,
`ctx.progress()`, `requireBotOwner()`, `command({ enabled: false })`.
