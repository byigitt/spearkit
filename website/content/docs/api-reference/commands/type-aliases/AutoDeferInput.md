---
title: "AutoDeferInput"
description: "How a handler opts into auto-defer: true for defaults, or fine-tuning."
---

> **AutoDeferInput** = `boolean` \| \{ `delayMs?`: `number`; `ephemeral?`: `boolean`; \}

Defined in: [src/auto-defer.ts:24](https://github.com/byigitt/spearkit/blob/main/src/auto-defer.ts#L24)

How a handler opts into auto-defer: `true` for defaults, or fine-tuning.
