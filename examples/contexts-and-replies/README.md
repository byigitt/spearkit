# Contexts and replies

The reply helpers shared by every handler (command, button, select, modal).

- [`reply-helpers.ts`](./reply-helpers.ts) — `reply`, `replyEphemeral`, `defer`/`editReply`, `send`, `followUp`, `error`, and the `{ ephemeral: true }` shortcut.

See also: [slash-commands](../slash-commands), [buttons](../buttons).
