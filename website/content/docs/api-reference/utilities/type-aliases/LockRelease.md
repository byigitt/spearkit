---
title: "LockRelease"
description: "Release a previously-acquired lease. Idempotent — safe to call multiple times."
---

> **LockRelease** = () => `void`

Defined in: [src/lock.ts:24](https://github.com/byigitt/spearkit/blob/main/src/lock.ts#L24)

Release a previously-acquired lease. Idempotent — safe to call multiple times.

## Returns

`void`
