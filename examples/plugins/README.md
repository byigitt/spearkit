# Plugins

Bundle related commands, events and components into a reusable unit.

- [`moderation.ts`](./moderation.ts) — `definePlugin`, including an async `setup`.
- [`using-plugins.ts`](./using-plugins.ts) — install plugins with `client.use(...)`.

See also: [file-based-loading](../file-based-loading), [getting-started](../getting-started).
